# URL pública do frontend (usada pelo app no browser)
VITE_API_URL=https://api.seu-dominio.com

# URL pública do backend .NET usada pela função serverless SEO (Vercel).
# OBRIGATÓRIO para o pré-render de SEO/sitemap funcionar em produção.
# Só funciona se a URL for absoluta (com https://).
SEO_API_URL=https://api.seu-dominio.com
